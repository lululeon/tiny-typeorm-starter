name: checks
on: [push, pull_request]
jobs:
  build:
    runs-on: "ubuntu-latest"
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: '14'
      - run: npm run lint
      - run: npm ci
  test:
    runs-on: "ubuntu-latest"
    # will this be sufficient
    needs: build
    steps:
      # temp: do nothing for now
      - run: npm run ci:installchk

